import 'package:flutter/material.dart';

const Color kTextColor = Colors.grey;
Color? kButtonBackgroundColor = Colors.grey[600];
Color? kGestureTextColor = Colors.grey[800];
Color? kIconColor = Colors.grey[600];

const Color kfillColor = Color(0xffe7edeb);

const double kFontSize = 20.0;
const double kSize = 8.0;

const kCustomButtonFixedSize = Size(200, 40);

const TextStyle kTextStyle = TextStyle(
  fontSize: kFontSize,
  color: kTextColor,
);

TextStyle kGestureTextStyle = TextStyle(
  fontSize: 16,
  fontStyle: FontStyle.italic,
  decoration: TextDecoration.underline,
  color: kGestureTextColor,
);

